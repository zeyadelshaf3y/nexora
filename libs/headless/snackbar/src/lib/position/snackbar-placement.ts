/**
 * Snackbar placement: viewport edges. Supports stacking per placement (e.g. multiple bottom-end).
 * Styled lib can use the same set for grouping and stacking.
 */
export type SnackbarPlacement =
  | 'top-start'
  | 'top'
  | 'top-end'
  | 'bottom-start'
  | 'bottom'
  | 'bottom-end';
