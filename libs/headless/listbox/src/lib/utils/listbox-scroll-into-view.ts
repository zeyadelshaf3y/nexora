/** Options for scrolling a non-virtual option element into view (native `scrollIntoView`). */
export const LISTBOX_OPTION_SCROLL_INTO_VIEW: ScrollIntoViewOptions = {
  block: 'nearest',
  inline: 'nearest',
};
