/** @internal Deep-import barrel; package consumers should use the library `index.ts` entry. */
export type {
  MentionItemsResult,
  MentionMatch,
  MentionInsertion,
  MentionSession,
  MentionTriggerConfig,
  MentionTriggerPanelOptions,
  MentionPanelState,
  MentionControllerState,
} from './mention-types';
