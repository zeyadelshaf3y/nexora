/**
 * Applied to the overlay pane wrapper (alongside `nxr-overlay-pane` and state classes).
 * Use selectors such as `.nxr-mention-overlay-pane.nxr-overlay-pane--entering`
 * for open/close motion styling.
 */
export const NXR_MENTION_OVERLAY_PANE_CLASS = 'nxr-mention-overlay-pane';

/** Default close-transition wait (ms) before detach; matches CSS `--closing` duration. */
export const DEFAULT_MENTION_CLOSE_ANIMATION_MS = 150;
