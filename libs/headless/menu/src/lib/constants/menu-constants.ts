/** Base overlay pane class for `<nxr-menu>`; merged with consumer `panelClass`. */
export const MENU_PANE_CLASS = 'nxr-menu-pane';

/** Default arrow size (px) when `displayArrow` is true and `arrowSize` is unset. */
export const MENU_DEFAULT_ARROW = { width: 12, height: 6 } as const;
